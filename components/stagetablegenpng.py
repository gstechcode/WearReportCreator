import PIL
from PIL import ImageFont
from pillowdrawtable.drawtable import Drawtable

class stagetablegenpng:
    def __init__(self, interval: list, data: dict) -> None:
        self.buildVars();
        
    
    def buildVars(self, op):
        
        if(op == "sup"):
            for i in 
        else:
            tdata= [
            ("18/17",'2'),
            ("19",100),
            ("20",10)
            ]
            